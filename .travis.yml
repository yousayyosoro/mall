# 自动构建文件
language: node_js
node_js:
  - "10"
cache: npm

branches:
  only:
    - master

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  verbose: true
  script:
    - npm run build
  on:
    branch: master
